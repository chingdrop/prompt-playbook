name: Playbook lint

on:
  pull_request:
  push:
    branches: [main]

jobs:
  playbook-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run playbook lint
        run: python scripts/playbook_lint.py
